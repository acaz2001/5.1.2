module.exports = {
    purge: [],
    darkMode: false, // or 'media' or 'class'
    theme: {
      extend: {
        fontFamilt: {
          sans: ['var(--font-inter)']
        }
      },
    },
    variants: {
      extend: {},
    },
    plugins: [],
  }